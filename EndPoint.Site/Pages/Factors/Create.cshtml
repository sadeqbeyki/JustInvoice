@page
@model EndPoint.Site.Pages.Factors.CreateModel
@{
    ViewData["Title"] = "ایجاد فاکتور جدید";
}

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">ایجاد فاکتور جدید</h3>
            </div>
            <div class="panel-body">
                <form method="post">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Factor.Name" class="control-label">نام</label>
                                <input type="text" class="form-control" asp-for="Factor.Name">
                                <span asp-validation-for="Factor.Name" class="error"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Factor.CreationDate" class="control-label">تاریخ ثبت</label>
                                <input class="form-control persianDateInput" asp-for="Factor.CreationDate">
                                <span asp-validation-for="Factor.CreationDate" class="error"></span>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="form-group no-margin">
                                <label asp-for="Factor.Description" class="control-label">توضیحات </label>
                                <textarea class="form-control autogrow" asp-for="Factor.Description"
                                          style="overflow: hidden; word-wrap: break-word; resize: horizontal; height: 50px;"></textarea>
                                <span asp-validation-for="Factor.Description" class="error"></span>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-12">
                        <table id="ExpTable" class="table table-striped table-sm">
                            <thead>
                                <tr>
                                    <th>ProductName</th>
                                    <th>UnitName</th>
                                    <th>Price</th>
                                    <th>Count</th>
                                    <th>+</th>
                                </tr>
                            </thead>
                            <tbody>
                                @*@foreach (var item in Model.Factor.Items)*@
                                @for (int i = 0; i < Model.Factor.Items.Count; i++)
                                {
                                    <tr>
                                        <td>
                                            @*<label asp-for="@Model.Factor.Items[i].ProductId" class="control-label">نام کالا</label>*@
                                            <select class="form-control" asp-for="@Model.Factor.Items[i].ProductId" asp-items="@Model.Products">
                                                <option value="0">نام کالا</option>
                                            </select>
                                        </td>
                                        <td>
                                            @*<label asp-for="@Model.Factor.Items[i].UnitId" class="control-label">واحد</label>*@
                                            <select class="form-control" asp-for="@Model.Factor.Items[i].UnitId" asp-items="@Model.Units">
                                                <option value="0">نام واحد </option>
                                            </select>
                                        </td>
                                        <td>
                                            @*@Html.EditorFor(x=>x.Factor.Items[i].Price,new htmlAttributes=new{@class="form-control"})*@
                                            <input asp-for="@Model.Factor.Items[i].Price" class="form-control" />
                                        </td>
                                        <td>
                                            <input asp-for="@Model.Factor.Items[i].Count" class="form-control" />
                                        </td>
                                        <td>
                                            <button id="btnadd" type="button" class="btn btn-sm btn-secondary visible">Add</button>
                                            <button id="btnremove" type="button" class="btn btn-sm btn-danger invisible">Delete</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <br />
                    <br />
                    <div class="form-group">
                        <button type="submit" value="Create" class="btn btn-success waves-effect waves-light m-b-5">ایجاد</button>
                        <a asp-page="./Index" class="btn btn-dark waves-effect waves-light m-b-5">بازگشت</a>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
	}



@*@for (int i = 0; i < Model.Factor.Items.Count; i++)
{
    <div class="row">
        <div class="col-md-3">
            <div class="form-group">
                <label asp-for="@Model.Factor.Items[i].ProductId" class="control-label">نام کالا</label>
                <select class="form-control" asp-for="@Model.Factor.Items[i].ProductId" asp-items="@Model.Products">
                    <option value="0">نام کالا</option>
                </select>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label asp-for="@Model.Factor.Items[i].UnitId" class="control-label">واحد</label>
                <select class="form-control" asp-for="@Model.Factor.Items[i].UnitId" asp-items="@Model.Units">
                    <option value="0">نام واحد </option>
                </select>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label asp-for="@Model.Factor.Items[i].Price" class="control-label">قیمت</label>
                <input type="text" class="form-control" asp-for="@Model.Factor.Items[i].Price">
                <span asp-validation-for="@Model.Factor.Items[i].Price" class="error"></span>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label asp-for="@Model.Factor.Items[i].Count" class="control-label">تعداد</label>
                <input type="text" class="form-control" asp-for="@Model.Factor.Items[i].Count">
                <span asp-validation-for="@Model.Factor.Items[i].Count" class="error"></span>
            </div>
        </div>
    </div>
}*@